export const environment = {
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseKey: 'YOUR_SUPABASE_ANON_KEY'
};

// Replace the values above with real Supabase credentials if you want the
// service to connect to a real backend. This file is intentionally minimal
// so the extracted component can be sent standalone.
